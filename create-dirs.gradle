/***
See: http://issues.gradle.org/browse/GRADLE-1289
*/

 
task "create-dirs" << {
   if(project.hasProperty('webAppDir')){
     webAppDir.mkdirs() 
   }
   println sourceSets.all*.hasProperty('groovy')
   if(sourceSets.all*.hasProperty('groovy').contains(true)){
    sourceSets.all*.groovy.srcDirs*.each { it.mkdirs() }   
   }else{
    sourceSets.all*.java.srcDirs*.each { it.mkdirs() }   
   }
   sourceSets.all*.resources.srcDirs*.each { it.mkdirs() }
}

